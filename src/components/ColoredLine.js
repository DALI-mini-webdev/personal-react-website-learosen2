import React from 'react'

const ColoredLine = ({ color }) => (
    <hr
        style={{
            color: 'black',
            backgroundColor: null,
            height: 5
        }}
    />
);

export default ColoredLine;